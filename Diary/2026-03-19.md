
2026-03-19 12:18:15  compgen 列出所有命令，类似 brew 这种，查询其清理缓存的命令 像这样brew cleanup --prune=all
，看看哪些可以清理缓存，把所有清理缓存的命令列出来。你根据我有的命令给建议

2026-03-19 14:36:58  这是 SSH 认证问题。Cargo 的内置 git 实现无法使用你的 SSH
  凭据。可以尝试以下解决方案：

  1. 使用 HTTPS 而不是 SSH（最简单）：
  cargo build --release -p gitbutler-git

  如果不行，在 ~/.cargo/config.toml 中添加：

  [http]
  proxy = "your-proxy-if-needed"

  [net]
  git-fetch-with-cli = true

2026-03-19 15:09:05  npx skills add https://github.com/tavily-ai/skills
